#!/bin/sh
# Compile and run a JS program
# $1 = filename
# $2 = extension (unused)

mkdir .cache
export DENO_DIR=$(pwd)/.cache

cp "$1" main.js
sudo -u "$KAMELI_RUNUSER" timeout -s KILL "$KAMELI_TIMELIMIT" deno run main.js
ECODE=$?
killall -9 -u "$KAMELI_RUNUSER" > /dev/null
exit $ECODE
